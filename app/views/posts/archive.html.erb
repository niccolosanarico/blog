<div class="col-xs-12 col-md-9">

<%= will_paginate %>

<ul class="posts">
  <% @posts.each do |p| %>
    <li>
  <p><%= link_to p.title, p %> - <%= p.published_at.nil? ? nil : p.published_at.strftime("%b %-d, %Y") %> 
  </li>
<% end %>
</ul>

<%= will_paginate %>

</div>
